apiVersion: apps/v1
kind: Deployment
metadata:
  name: erp-ai-governance
  labels:
    app.kubernetes.io/name: erp-ai-governance
    app.kubernetes.io/part-of: erp-platform
    app.kubernetes.io/component: ai-governance
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: erp-ai-governance
  template:
    metadata:
      labels:
        app.kubernetes.io/name: erp-ai-governance
        app.kubernetes.io/part-of: erp-platform
    spec:
      serviceAccountName: erp-workload
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ai-governance
          image: ghcr.io/thiagodifaria/erp-ai-governance:1.4.6
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8108
          envFrom:
            - configMapRef:
                name: erp-runtime-config
            - secretRef:
                name: erp-runtime-secrets
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
