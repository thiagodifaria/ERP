# identity

The identity service owns tenants, users, teams, roles and access foundations.

Initial scope:

- minimal API bootstrap
- project boundaries by layer
- health endpoints
- domain seed types for tenancy growth
