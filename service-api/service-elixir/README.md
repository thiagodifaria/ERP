# service-elixir

Elixir is reserved for durable workflow execution and high-resilience runtime automation.

Planned services:

- workflow-runtime

Already started:

- `workflow-runtime`

Standard layout:

- `lib/<service>/application.ex`
- `lib/<service>/server.ex`
- `lib/<service>/api`
- `lib/<service>/application`
- `lib/<service>/domain`
- `lib/<service>/infrastructure`
- `lib/<service>/telemetry`
- `test/unit`
- `test/integration`
- `test/contract`
